parameters:
  secret: ''

phases:
- phase: FromTemplate
  queue: Hosted VS2017
  variables:
    sec: ${{ parameters.secret }}
  steps:
  - script: "echo Secret=$(sec)"
    displayName: Display Secret Value Directly